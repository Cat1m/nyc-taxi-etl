# Python
venv/
__pycache__/
*.pyc

# DuckDB warehouse file
*.duckdb
*.duckdb.wal

# Raw / staging data (regenerated by ingestion scripts, too large for git)
data/raw/*
!data/raw/.gitkeep
data/staging/*
!data/staging/.gitkeep

# dbt artifacts
target/
dbt_packages/
logs/
.user.yml

# Airflow
airflow.db
airflow.cfg
airflow-webserver.pid

# Secrets / local env
.env
# ngoại lệ: orchestration/.env chỉ chứa AIRFLOW_UID, không phải secret thật
!orchestration/.env
