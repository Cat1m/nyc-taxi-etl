name: "nyc_taxi_etl"
version: "1.0.0"
config-version: 2

profile: "nyc_taxi_etl"

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  nyc_taxi_etl:
    staging:
      +materialized: view
    marts:
      +materialized: table
